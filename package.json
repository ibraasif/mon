{
    "name": "mon",
    "version": "0.0.1",
    "description": "Bengali for mind (মন). Cloudflare-native AI memory layer.",
    "main": "src/index.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}